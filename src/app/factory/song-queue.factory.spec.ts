import { DefaultSongQueueFactory } from './song-queue.factory';

describe('SongQueue Factory', () => {
  let factory;

  beforeEach(() => {
    factory = new DefaultSongQueueFactory();
  });

  it('should create queues', () => {
    // TODO
  });
});
